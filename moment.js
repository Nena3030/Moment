const moment = require("moment");

let y;

y = moment ().format ('YYYY-MM-DD')//ja pecati godinata vo format godina mesec den
y = moment ().format ("DD-MM-YYYY")//den mesec godina



console.log (y);

